name: Publish to NPM Registry

permissions:
  id-token: write

on:
  push:
    branches:
      - main

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/components' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@e8cb7e795bc27e0bd323135e17832c5f3e36f38a # v2.3.3
