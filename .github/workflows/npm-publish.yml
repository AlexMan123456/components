name: publish npm

on:
  push:
    branches:
      - main

jobs:
  publish:
    uses: AlexMan123456/github-actions/npm-publish/action.yml@v1.1.1
    with:
      NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
