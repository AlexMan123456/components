name: Publish to NPM Registry

permissions:
  id-token: write

on:
  push:
    branches:
      - main

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/components' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@c016c13a6227b8a31b5ddd866063bb5362873e70 # v3.0.1
