name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: AlexMan123456/github-actions/npm-ci@4d51e9fa5acb173de967108f078c9af7e0fa0450 # v1.3.6
        with:
          run-tests: false
