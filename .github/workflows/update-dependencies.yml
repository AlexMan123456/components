name: Update Dependencies
on:
  workflow_dispatch:
  schedule:
  - cron: 0 21 * * 5

permissions:
  contents: write
  pull-requests: write

jobs:
  update-dependencies:
    if: ${{ github.repository == 'AlexMan123456/components' }}
    uses: AlexMan123456/github-actions/.github/workflows/update-dependencies.yml@e8cb7e795bc27e0bd323135e17832c5f3e36f38a # v2.3.3
    secrets:
      PAT_GITHUB: ${{ secrets.PAT_GITHUB }}
